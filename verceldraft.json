{ 
    "version": 2, 
    "routes":[
        {
            "src": "/(.*)",
            "dest": "/api",
            "method": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
            "headers": {
                "Access-Control-Allow-Origin": "*"
            }
        }
    ]
}